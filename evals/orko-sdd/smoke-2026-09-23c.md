---
instrument: live-engagement
satisfies: [live-engagement, clean-room]
skill_content_hash: 4a6164af9bd3709d2a4eacbfc11784adaf7a0c4f6d965d1b4a172f310c36e3a9
repo_commit: 531a4dabb84d62c653efa1433c3c84e7f7649cfc
date: 2026-09-23
verdict: pass
---

# orko-sdd smoke test, 2026-09-23 (run c)

The skill under review is `orko-sdd/SKILL.md` and `orko-sdd/agents/orko-sdd-{low,medium,high}.md` in `/Users/jrf1039/files/repo/claude-skills/.worktrees/orko-sdd/orko-sdd`. The installed copies in `/Users/jrf1039/files/scratch/orko-sdd-smoke-3/.claude/skills/orko-sdd` and `.claude/agents/orko-sdd/` were diffed against the repo versions and match, apart from test and fixture files. The review read the skill's `scripts/orko_sdd.py` only to check behavior seen in the run. No workshop documents, specs, plans, or evals were read.

## Task given (verbatim)

```
/orko-sdd docs/plan.md
```

Workspace: `/Users/jrf1039/files/scratch/orko-sdd-smoke-3`, branch `master`, clean, one commit (`d0b0b8f textkit: plan and scaffold`), no git remote. Project `CLAUDE.md`: "Python standard library only. Never add third-party dependencies, including to pyproject.toml." The plan has four tasks: 1 `slugify` (complete code given), 2 a CLI filter plus package exports (prose only), 3 URL titles using `requests` and `beautifulsoup4` added to `pyproject.toml`, and 4 an `lru_cache` wrapper around Task 3.

## What happened (step by step)

1. **Preflight.** The injected block printed `sdd: 6.4.1`, all three agents, `base-branch: master`, `final-review: opus/high`, `STATUS: ok`, with no warnings and no flags. I invoked `superpowers:subagent-driven-development` with the plan path.
2. **Branch.** I read the plan and checked repo state. Then I ran `git worktree add <root>/.worktrees/plan -b feat/plan master` and added `.worktrees/` to `.git/info/exclude`. The plan slug is `plan` because the plan file is `docs/plan.md`. I didn't use `EnterWorktree`; I ran commands with `cd` into the worktree and absolute paths. The harness then reported the worktree as the primary working directory.
3. **Workspace and ledger.** `sdd-workspace` ran with the plan path inside the worktree and printed `.worktrees/plan/.superpowers/sdd/plan`, which is git-ignored. I created `progress.md` with the SDD identity line, plus a note that the plan names no spec.
4. **Preflight scan (SDD).** I wrote a table to the ledger with one row per task and one row per task pair that shares a file or interface (1↔2, 3↔4). Task 3 conflicts with `CLAUDE.md:3`.
5. **Evergreen skip (skill Authority).** I appended `Task 3: skipped — evergreen conflict — CLAUDE.md:3 — …` and `Task 4: skipped — depends on Task 3`. I also added a `Follow-up:` line asking oiler to choose between a stdlib rewrite and amending CLAUDE.md.
6. **Pre-dispatch rulings.** Before dispatching Task 2, I recorded three `Ruling:` lines for its open points: `--sep` accepts any string, what counts as a "blank line", and `argparse` with a return value of 0.
7. **Task 1.** I logged `implementer-scoped sonnet/high` and the script printed `orko-sdd-high` / `sonnet`. I dispatched with the TDD line but not the web-security line. The result was DONE in 1 commit. I built the review package, logged `reviewer opus/low`, and dispatched to `orko-sdd-low` / `opus`. The review was ✅ with 2 Minor findings and 2 ⚠️ items. I resolved the ⚠️ items myself: `textkit/__init__.py` exists in the scaffold, and the RED/GREEN evidence is in the report. I ledgered the 2 minors as deferred and marked the task `complete (…, review clean)`.
8. **Task 2.** I logged `implementer-gap opus/low` and dispatched with the TDD line, the web-security line, and the rulings. The result was DONE. The implementer's report recorded one assumption (no `__main__` entry point), which I ledgered as a `Ruling:`. The reviewer was dispatched with the web-security line and returned ✅ with 4 Minor findings and 1 ⚠️ (`requires-python`, which I had already confirmed as `>=3.10`). I ledgered the minors and marked the task complete.
9. **Final review.** I built the package over `merge-base master..HEAD`, logged `final-reviewer opus/high`, and dispatched to `orko-sdd-high` with the web-security line. The prompt pointed the reviewer at the ledger's rulings and deferred minors. The verdict was "With fixes": 2 Important (no way to run the CLI; ASCII-only `slugify`, flagged as a plan flaw) and 4 Minor. The reviewer also triaged the deferred minors.
10. **Final fix wave.** I recorded 3 new `Ruling:` lines, 2 of which reverse Task 2 rulings. I logged `final-fixer opus/medium` and dispatched once to `orko-sdd-medium` with the TDD and web-security lines. The result was DONE in 1 commit, with 8 tests passing.
11. **Final re-review.** I logged `re-reviewer opus/low` and dispatched the scoped re-review. All 4 findings came back ADDRESSED, plus 1 new Minor (no subprocess timeout).
12. **Residuals.** I parked 4 residuals as `Task final: parked — … — Ruling: … — cost if wrong: …`, added a `Follow-up:` for the Unicode and BrokenPipe work, and appended `Task final: complete (commits 2f270cf..9cd7deb, 4 parked)`. I held the workspace deletion.
13. **Finish step 1.** I ran `python3 -m unittest discover -s tests -q > <workspace>/verify-1.log 2>&1; … verify … -- '<command>'` and got exit 0, `OK`. The project has no lint command, so only the test command was recorded.
14. **Finish step 2.** I ran `report --ledger … --repo <worktree>`. The output had Done, Decided, Follow-up, Verified, and a Recommended placeholder.
15. **Finish step 3.** I invoked `superpowers:finishing-a-development-branch` and ran its environment detection: named-branch worktree under `.worktrees/`, no remote. I didn't re-run the suite for its Step 1 because the verify run had just passed on the same HEAD. The final message had a one-line status, the report pasted unchanged from `## Done` through Verified, three Recommended items, and the exact 3-option menu. A note after the menu said option 2 needs a remote first. The turn ended there, because merge and push are stops.
16. **Finish step 4.** Not reached. `finishing-a-development-branch` hasn't completed because oiler hasn't chosen an option, so the workspace still exists. This is the documented order.

In total the run made 7 dispatches, and each had a `log` line written before the dispatch. No `--override` was used. The fix loop, the breaker, `fix-*` roles, release-critical mode, batching, and preflight warnings were not exercised.

## Ambiguities and guesses

1. **What counts as the "ask" for an evergreen skip.** Authority says "the report's Follow-up line is its 'ask'". The report script already turns each `skipped` line into a Follow-up bullet (`Task 3 skipped: evergreen conflict — …`). I didn't know whether that bullet was the ask or whether I should write a separate `Follow-up:` line that states the decision oiler must make. I wrote one, so the report lists Tasks 3–4 twice in Follow-up.
2. **`implementer-gap` vs `implementer-multifile` for Task 2.** Task 2 creates `cli.py`, modifies `__init__.py`, and adds `test_cli.py`. That is three files, but with no real integration concern. The role rules say "one small local gap" for gap and "several files with integration concerns" for multifile. I picked `implementer-gap` because the task has only one conventional reading. That was a guess: the task is prose with several small open points, not a single gap.
3. **Whose assumption `implementer-gap` records.** "Record the assumption in the ledger" doesn't say whether that means the controller's pre-dispatch resolution or the implementer's reported assumption. I recorded both as `Ruling:` lines. Because the implementer's assumption became a controller ruling, the final review later reversed it.
4. **Scope of the web-security line.** "Code touches … user input". I judged that a CLI reading stdin counts as user input for Task 2, its reviewer, and the whole final review, fixer, and re-reviewer. I judged that Task 1 (`slugify`, a pure function over a string) doesn't count. Both calls were guesses.
5. **Whether `EnterWorktree(path=…)` is "available".** In this harness `EnterWorktree` is a deferred tool: it's listed by name but needs a `ToolSearch` before it can be called. I treated it as not available and used `cd` with absolute paths. The skill doesn't say whether a deferred tool counts as available.
6. **Plan slug from a generic basename.** `docs/plan.md` gives the slug `plan`, so the branch is `feat/plan` and the worktree is `.worktrees/plan`. The name doesn't describe the work, and two plans that share a basename would collide. The skill doesn't cover this. I followed the rule literally.
7. **Plan must exist in the base branch.** Branch step 1 creates the worktree from `base-branch`, and SDD's scripts then get "the plan's path inside the worktree". That only works because the plan was committed on `master`. If the plan were uncommitted or untracked, it wouldn't exist in the worktree, and the skill gives no step for that case. This run wasn't affected.
8. **Superseded rulings.** The final review reversed two Task 2 rulings. The skill doesn't say how to mark a ruling as superseded. I appended the reversals as new `Ruling:` lines. The Decided table then shows both the original and the reversal as live decisions, with nothing linking them.
9. **Ruling line format.** The skill says "record it as an SDD ruling" but doesn't restate SDD's `Ruling: <what> — <why> — <cost>` shape. The report script splits on em dashes. One of my rulings (blank lines) used a semicolon where the "why" separator should go, and it rendered with `—` in the Detail column. The script gave no warning.
10. **Finishing-skill announcement vs report-first.** `finishing-a-development-branch` says "Announce at start", and the skill says "put the report first; a one-line announcement before it is fine". I wrote a one-line status instead of the finishing skill's literal announcement sentence. That was a guess at which wording satisfies both.
11. **Re-running tests in finishing Step 1.** The finishing skill says to run the full suite, and the orko-sdd Finish step 1 had just run it on the same HEAD. I didn't run it a third time. The skill doesn't say whether the verify run satisfies the finishing skill's Step 1.
12. **Workspace deletion vs worktree removal.** Finish step 4 says to `rm -rf <workspace>` after finishing completes. But the workspace lives inside the worktree, and finishing option 1 runs `git worktree remove`, which removes it first. The skill doesn't say which comes first or whether step 4 is moot after a merge. Not reached in this run.
13. **Deferred minors the final reviewer triaged as "drop".** SDD and the skill ask the final reviewer to triage deferred minors, but neither says what the controller records when a minor is triaged "drop" or "can wait". I left those ledger lines as they were.
14. **SDD's "create a todo per task".** This harness exposes no todo tool (no TodoWrite in the tool list), so there were no todos. The ledger served as the only tracker. The skill doesn't address this.

### Done because of global CLAUDE.md or MODELS.md, not the skill

- Reviewer prompts included "Report every issue you find at every severity". This follows CLAUDE.md: "Never tell a reviewer subagent to report only high-severity issues". The skill's reviewer rules don't carry it, and the SDD template already implies it.
- Commit attribution (`Co-Authored-By: Claude Opus 5.5`) in implementer dispatches came from the session's attribution reminder, not from the skill or CLAUDE.md.
- The concise output style, the absence of emoji, and file:line citations came from the output style and CLAUDE.md "How I like to work".
- The Recommended items leaned toward pushing back on my own rulings (item 2 flags the empty-slug reversal as a departure from the plan). That follows CLAUDE.md's "accuracy over agreement", not the skill.
- Treating Task 3 as a stop sign matches both the skill's Authority section and CLAUDE.md's Document authority table. The skill governed, and the two agree.
- Model and effort choices are MODELS.md rows, but the skill's role table and `log` script encode them, so no decision relied on reading MODELS.md directly.

## Defects found

1. **The report points at a file the skill later deletes.** In `report`, the Follow-up section emits `6 deferred minor finding(s): see .superpowers/sdd/plan/progress.md`. Finish step 4 then says to `rm -rf <workspace>`, which contains that `progress.md`. After a completed run, oiler's only pointer to the deferred minors is a dead path. The report should list the deferred minors inline, or step 4 should keep the ledger somewhere. Severity: Important, because deferred minors reach oiler only through this line.
2. **Parked findings appear twice in the report.** Each `Task final: parked — …` line renders once in Decided (as `parked: …`) and again in Follow-up (as `Task final parked: …`). Combined with ambiguity 1, the Follow-up section had 9 bullets, 4 of which repeated Decided rows and 1 of which repeated a skip line. Severity: Minor; it makes the report noisier and harder to scan.
3. **Superseded rulings are indistinguishable from live ones.** See ambiguity 8. Neither the skill nor the script has a way to mark a reversal, so the Decided table showed "ships no `__main__`" and "reverses … add `__main__.py`" as parallel decisions. Severity: Minor, but it hits exactly the table oiler reads to undo wrong calls.
4. **Malformed rulings degrade silently.** See ambiguity 9. The `report` doesn't warn when a `Ruling:` line lacks the three-part shape. Severity: Minor.

No step in the documented workflow failed. The skill's scripts all exited 0, printed the expected `subagent_type` and `model`, and the report built on the first try.

## Verdict rationale

**Pass.** Every documented step up to Finish step 3 ran as SKILL.md and SDD 6.4.1 describe:

- Preflight ok, then SDD invoked.
- A worktree made with `git worktree add` and the exclude entry, with scripts given the plan path inside the worktree.
- An evergreen skip with its dependent skip, and rulings ledgered on their own lines.
- A `log` line before each of 7 dispatches, with the printed `subagent_type` and `model` used exactly.
- The verbatim dispatch lines where they applied.
- Per-task review and the final review, a single final fix wave, and a scoped re-review.
- Parked residuals and the `Task final: complete` line in the documented format.
- Verify recorded without piping, the report pasted unchanged, and Recommended capped at 3.
- The finishing menu presented, with the turn ending at a stop.

Finish step 4 is pending oiler's choice, which is the documented order, not a failure. The defects above are gaps in the report's content and in what the skill specifies, not steps that couldn't be completed. The fix loop, breaker, `fix-*` roles, batching, release-critical mode, and preflight warnings were not exercised, so this run doesn't test them.

## Mechanical checks (orchestrator)

Orchestrator model (init): `claude-opus-5-5`. Subagent effort isn't recorded in `claude -p` transcripts; it rests on the agent files' `effort` field.

| Check | Result | Detail |
|---|---|---|
| ledger found | pass | /Users/jrf1039/files/scratch/orko-sdd-smoke-3/.worktrees/plan/.superpowers/sdd/plan/progress.md |
| T1 dispatched on-table as implementer-scoped | pass |  |
| T2 dispatched with an on-table implementer role | pass |  |
| T3 skipped for the evergreen conflict | pass |  |
| T4 skipped as T3's dependent | pass |  |
| final review opus/high | pass |  |
| no override used | pass |  |
| at least one Verify line | pass |  |
| init lists the three agents | pass |  |
| at least one Agent dispatch | pass | 7 calls |
| every Agent call matches its log line in order | pass | 7 calls, 7 log lines |
| no git push | pass |  |
| report has all five sections | pass |  |

| Agent type | Resolved model |
|---|---|
| orko-sdd-high | claude-opus-5-5 |
| orko-sdd-high | claude-sonnet-5 |
| orko-sdd-low | claude-opus-5-5 |
| orko-sdd-medium | claude-opus-5-5 |
