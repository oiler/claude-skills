[project]
name = "guardian-claude-code-tests"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "packaging>=24.0",
    "pytest>=8.0",
    "respx>=0.21",
]

[tool.pytest.ini_options]
testpaths = [".."]
python_files = ["test_*.py"]
pythonpath = ["..", "../scripts"]
